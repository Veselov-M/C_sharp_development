using System;

namespace Test
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("My homework is divided into 2 parts and this is first" );
            Console.Write("\nPlease enter your value for x ");

            string a = Console.ReadLine();
            int x = Convert.ToInt32(a);

            Console.Write("Now please enrer your value for y ");

            string b = Console.ReadLine();
            int y = Convert.ToInt32(b);


            Console.WriteLine("\nBased on the data obtained, we have the following answers ");
            Console.WriteLine();

            int c = (-6 * x * x * x) + (5 * x * x) - (10 * x) + 15;              //Math.Pow(x, 3) видає помилку

            Console.WriteLine("\n-6 * x^3 + 5 * x^2 - 10*x + 15 = " + c);
            Console.WriteLine("abs(x) * sin(x) = " + (Math.Abs(x) * Math.Sin(x)));
            Console.WriteLine("2 * pi * x = " + (2 * Math.PI * x));
            Console.WriteLine("Max(x, y) = " + Math.Max(x,y));

            Console.WriteLine("\nPart №2");

            Console.Write("\nPlease enter the start year (please enter number less than 2023 and greater than 0) - ");

            string e = Console.ReadLine();
            int yearBefore = Convert.ToInt32(e);

            Console.Write("Please enter the end year (please enter number greater than 2023) - ");

            string g = Console.ReadLine();
            int yearAfter = Convert.ToInt32(g);

            int todayYear = DateTime.Now.Year;

            if (yearBefore > todayYear || yearBefore < 0)
            {
                Console.WriteLine("You enter invalid number, please restart the program and try again");
            }
            else if (yearAfter < todayYear)
            {
                Console.WriteLine("You enter invalid number, please restart the program and try again");
            }

            else
            {
                Console.WriteLine("\nActual year is " + todayYear);
                Console.WriteLine("From your start year gone " + (todayYear - yearBefore) + " years");
                Console.WriteLine("To the year you entered as the last one left " + (yearAfter - todayYear));
                Console.WriteLine(Console.ReadLine());               //Аби консоль не закрилась до виводу останнього речення
            }
        }
    }
}
